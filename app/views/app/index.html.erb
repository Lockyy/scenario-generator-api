
<% flash.each do |name, msg| -%>
  <% name = name == 'notice' ? 'success' : name  %>
  <%= content_tag :div, msg, class: "alert alert-#{name}" %>
<% end -%>

<div id="content" data-auth-token='<%= @auth_token %>'></div>

